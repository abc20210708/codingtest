package com.example.sqlkit.lv20;

//lv2. 동명 동물 수 찾기
public class Q21 {
    /*
     SELECT NAME, COUNT(NAME)
    FROM ANIMAL_INS
    GROUP BY NAME
    HAVING COUNT(NAME) >= 2
    ORDER BY NAME
    * */
}
